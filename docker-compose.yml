services:
  app:
    build: .
    container_name: parley-chat-sova
    restart: "unless-stopped"
    volumes:
      - ./config.toml:/app/config.toml
      - ./data:/app/data
      - ../Mura:/Mura
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - parley-chat-network
    expose:
      - "42835"
    healthcheck: # The health check URL must contain the URI Path if set in config.toml
      test: ["CMD", "curl", "-f", "http://localhost:42835/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  nginx:
    image: nginx:alpine
    container_name: parley-chat-nginx
    restart: "unless-stopped"
    ports:
      - "42835:42835" # To change the port, change the first part of the port before :
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/nginx/certs:ro
    depends_on:
      app:
        condition: service_healthy
    networks:
      - parley-chat-network

networks:
  parley-chat-network:
    driver: bridge